---
title: "安装python"
description: "安装python与常用开发工具链"
when: "python环境缺失时"
tags:
  - python
  - pixi
  - uv
uri: ""
---

import { Code } from 'astro-expressive-code/components';

export const MIRROR_CONDA = (import.meta.env.MIRROR_CONDA ?? "https://mirrors.tuna.tsinghua.edu.cn/anaconda").replace(/\/$/, '')
export const MIRROR_PYPI = (import.meta.env.UV_DEFAULT_INDEX ?? "http://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple").replace(/\/$/, '')

export const pixi = `
default-channels=["main"]
[mirrors]
"https://conda.anaconda.org/main" = ["${MIRROR_CONDA}/pkgs/main"]
"https://conda.anaconda.org/msys2" = ["${MIRROR_CONDA}/pkgs/msys2"]
"https://conda.anaconda.org/conda-forge" = ["${MIRROR_CONDA}/cloud/anaconda"]
"https://conda.anaconda.org/pytorch" = ["${MIRROR_CONDA}/cloud/pytorch"]
"https://conda.anaconda.org/pytorch3d" = ["${MIRROR_CONDA}/cloud/pytorch3d"]
[pypi-config]
index-url="${MIRROR_PYPI.replace(/\/simple$/, '')}"
[repodata-config]
disable-sharded=true # avoid 404
disable-zstd=true # avoid EOF
[shell]
change-ps1=true
`;

export const uv = `
[[index]]
url="${MIRROR_PYPI}"
`;

## how

### pixi+uv+python

1. 在 https://github.com/prefix-dev/pixi/releases 下载最新版的 [pixi-x86_64-pc-windows-msvc.zip](https://github.com/prefix-dev/pixi/releases/latest/download/pixi-x86_64-pc-windows-msvc.zip)
2. 将解压后的 `pixi.exe` 放入 $env:PATH _(环境变量的PATH)_

3. 设置镜像

<Code code={pixi} lang="toml" title="%USERPROFILE%/.pixi/config.toml" mark={MIRROR_CONDA} />
<Code code={uv} lang="toml" title="%APPDATA%/uv/uv.toml" mark="simple" />

:::note[配置文件路径]

在 mac OS/linux 中，则分别为 `~/.config/pixi/config.toml` 与 `~/.config/uv/uv.toml`

:::

4. 调用pixi安装uv与python
```powershell
pixi global install uv python -e uv
```

## why

更加现代的包管理器，能在一定程度上加速我们的开发过程，包括但不限于 环境隔离[^环境隔离]、加速依赖解析&下载&安装、更加智能的报错……

[^环境隔离]: 如项目A的python为版本3.8，而项目B的python为版本3.10，传统的pip会默认尝试让2个项目使用相同的安装环境，一般会报错。而uv默认就像`pip venv`一样会直接创建独立环境。

以前只有一个全局环境，容易遇到包版本的冲突；现在像npm的node_modules一样，支持且默认可以为每个项目独立安装依赖包。

conda -> [mamba](https://github.com/mamba-org/mamba) -> [pixi](https://github.com/prefix-dev/pixi)

pip -> [uv](https://docs.astral.org.cn/uv/)

### conda 与 pypi 包的区别

| 维度       | PyPI (`pip`)                          | Conda (`conda`)                                                      |
| :--------- | :------------------------------------ | :------------------------------------------------------------------- |
| 语言范围   | Python Only                           | Any Language (Python, C, C++, R, Fortran, Rust等)                    |
| 文件类型   | `.py` / `.whl` / `.so` / `.pyd`       | 上述所有 + `.dll` / `.dylib` / `.a` + `.h` / `.cmake` + 可执行文件等 |
| Python入口 | ✅ 必须有 (至少包含 `.py` 或入口声明) | ❌ 不需要 (可以完全没有 Python 文件)                                 |
| 依赖管理   | 仅解析 Python 包依赖                  | 全栈解析 (Python + C/C++库 + Fortran运行时 + R包等)                  |
| 包体积     | 较小 (仅含 Python 相关产物)           | 较大 (包含完整二进制、运行时，但底层库可跨包共享)                    |
| 典型场景   | 纯 Python 项目、Web开发、通用脚本     | 数据科学、科学计算、跨语言栈、复杂编译依赖项目                       |
| 本质定位   | Python 专属的包仓库                   | 语言无关的通用包/环境管理器 (Python只是其中一种生态)                 |
