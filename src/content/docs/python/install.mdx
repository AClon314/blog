---
title: "安装python"
description: "安装python与常用开发工具链"
when: "python环境缺失时"
tags:
  - python
  - pixi
  - uv
uri: ""
---
import { Steps } from '@astrojs/starlight/components';
import Code from '/src/components/Code.astro';
import Only from '/src/components/Only.astro';

export const CONDA_MIRROR = (import.meta.env.CONDA_MIRROR ?? "https://mirrors.tuna.tsinghua.edu.cn/anaconda").replace(/\/$/, '')
export const PYPI_MIRROR = (import.meta.env.UV_DEFAULT_INDEX ?? "http://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple").replace(/\/$/, '')

export const pixi = `
default-channels=["main"]
[mirrors]
"https://conda.anaconda.org/main" = ["${CONDA_MIRROR}/pkgs/main"]
"https://conda.anaconda.org/msys2" = ["${CONDA_MIRROR}/pkgs/msys2"]
"https://conda.anaconda.org/conda-forge" = ["${CONDA_MIRROR}/cloud/anaconda"]
"https://conda.anaconda.org/pytorch" = ["${CONDA_MIRROR}/cloud/pytorch"]
"https://conda.anaconda.org/pytorch3d" = ["${CONDA_MIRROR}/cloud/pytorch3d"]
[pypi-config]
index-url="${PYPI_MIRROR.replace(/\/simple$/, '')}"
[repodata-config]
disable-sharded=true # avoid 404
disable-zstd=true # avoid EOF
[shell]
change-ps1=true
`;

export const uv = `
[[index]]
url="${PYPI_MIRROR}"
`;

## how

### pixi+uv+python

<Steps>

1. 在 https://github.com/prefix-dev/pixi/releases 下载最新版的 [pixi-x86_64-pc-windows-msvc.zip](https://github.com/prefix-dev/pixi/releases/latest/download/pixi-x86_64-pc-windows-msvc.zip)
2. 将解压后的 `pixi.exe` 放入 环境变量PATH的范围内

3. 设置镜像

    <Code
      code={pixi}
      lang="toml"
      title="%USERPROFILE%/.pixi/config.toml"
      title-mac="~/.config/pixi/config.toml"
      title-linux="~/.config/pixi/config.toml"
      mark={CONDA_MIRROR}
    />

    <Code
      code={uv}
      lang="toml"
      title="%APPDATA%/uv/uv.toml"
      title-mac="~/.config/uv/uv.toml"
      title-linux="~/.config/uv/uv.toml"
      mark="simple"
    />

4. 调用pixi安装uv与python

    ```powershell
    pixi global install uv python ipython -e uv
    uv python list
    ```
    输出包含 `.pixi/env/uv/python` 则安装成功

    :::tip
    pixi升级已安装的python
    ```powershell
    pixi global list # 查看现有的全局子环境
    pixi global update uv # 升级现有的uv环境
    ```
    :::

    :::caution
    可选步骤: 将项目级的.venv默认激活以作为全局环境

    某些情况下你可能会执行`pixi global uninstall uv`，但这个指令也会删除你之前安装的python包。
    为了避免重新安装的麻烦，可以单独创建一个venv的python级虚拟环境，然后在 powershell 初始化时就进入该uv环境即可。
    :::

5. 将刚才 第4步 获得的 `.pixi/env/uv`的完整地址 加入 环境变量PATH的范围

6. 🎉 现在你可以执行 `python` 了，用 `uv pip install ...` 来安装python包吧

</Steps>

## why

### 为什么不在[python.org](https://www.python.org/)官网上下载与安装?

更加现代的包管理器，能在一定程度上加速我们的开发过程，包括但不限于 环境隔离[^环境隔离]、加速依赖解析&下载&安装、更加智能的报错……

[^环境隔离]: 如项目A的python为版本3.8，而项目B的python为版本3.10，传统的pip会默认尝试让2个项目使用相同的安装环境，一般会报错。而uv默认就像`pip venv`一样会直接创建独立环境。

以前只有一个全局环境，容易遇到包版本的冲突；现在像npm的node_modules一样，支持且默认可以为每个项目独立安装依赖包。

conda -> [mamba](https://github.com/mamba-org/mamba) -> [pixi](https://github.com/prefix-dev/pixi)

pip -> [uv](https://docs.astral.org.cn/uv/)

### conda 与 pypi 包的区别

| 维度       | PyPI (`pip`)                          | Conda (`conda`)                                                      |
| :--------- | :------------------------------------ | :------------------------------------------------------------------- |
| 语言范围   | Python Only                           | Any Language (Python, C, C++, R, Fortran, Rust等)                    |
| 文件类型   | `.py` / `.whl` / `.so` / `.pyd`       | 上述所有 + `.dll` / `.dylib` / `.a` + `.h` / `.cmake` + 可执行文件等 |
| Python入口 | ✅ 必须有 (至少包含 `.py` 或入口声明) | ❌ 不需要 (可以完全没有 Python 文件)                                 |
| 依赖管理   | 仅解析 Python 包依赖                  | 全栈解析 (Python + C/C++库 + Fortran运行时 + R包等)                  |
| 包体积     | 较小 (仅含 Python 相关产物)           | 较大 (包含完整二进制、运行时，但底层库可跨包共享)                    |
| 典型场景   | 纯 Python 项目、Web开发、通用脚本     | 数据科学、科学计算、跨语言栈、复杂编译依赖项目                       |
